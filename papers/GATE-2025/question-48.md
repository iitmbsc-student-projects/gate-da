---
title: Question-48
pagetitle: Question-48
order: 48
categories: [calculus, derivatives, neural networks, activation function, DA-2025]
---

Which of the following statements is/are correct about the rectified linear unit (ReLU) activation function defined as $\text{ReLU}( x) =\max( x,0)$, where $x\in \mathbb{R}$?

 - [ ] ReLU is continuous everywhere

 - [ ] ReLU is differentiable everywhere

 - [ ] ReLU is not differentiable at $x=0$

 - [ ] $\text{ReLU}( x) =\text{ReLU}( ax) ,$for all $a\in \mathbb{R}$

::: {.callout-note title="Answer" collapse=true}

 - [x] ReLU is continuous everywhere

 - [ ] ReLU is differentiable everywhere

 - [x] ReLU is not differentiable at $x=0$

 - [ ] $\text{ReLU}( x) =\text{ReLU}( ax) ,$for all $a\in \mathbb{R}$

:::

::: {.callout-note title="Solution" collapse=true}

ReLU is piece-wise linear. It is continuous everywhere but not differentiable at $x=0$. The last option is clearly incorrect.

:::